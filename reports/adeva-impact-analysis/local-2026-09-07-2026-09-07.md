## 🗺️ Análisis de Impacto — trabajo local sin PR (Vitest + docs + fix de stores, 2026-09-07)

**Entorno:** con file read (repositorio completo disponible)
**Fuente de contexto:** `contexts/SYSTEM_CONTEXT.md` (actualizado 2026-09-07, mismo día del cambio)
**Confianza del análisis:** 🟢 Alta

---

### Cambios directos

| Archivo | Tipo | Entidades públicas afectadas | Tipo de cambio |
|---|---|---|---|
| `package.json` | modificado | scripts `test`/`test:watch`/`test:coverage`; devDependencies (`vitest`, `@vue/test-utils`, `happy-dom`, `@vitest/coverage-v8`) | config |
| `vitest.config.ts` | agregado | configuración de test runner | config |
| `src/http/index.ts` | modificado | ninguna (solo JSDoc) | additive (documentación) |
| `src/services/CategoryService.ts` | modificado | ninguna (solo JSDoc) | additive |
| `src/services/CheckoutService.ts` | modificado | ninguna (solo JSDoc) | additive |
| `src/services/DealService.ts` | modificado | ninguna (solo JSDoc) | additive |
| `src/services/ProductService.ts` | modificado | ninguna (solo JSDoc) | additive |
| `src/stores/category.ts` | modificado | `useCategoryStore()` — objeto retornado ahora incluye `error` | **signature** (ampliación del contrato del store) |
| `src/stores/deal.ts` | modificado | `useDealStore()` — objeto retornado ahora incluye `error` | **signature** |
| `src/stores/product.ts` | modificado | `useProductStore()` — objeto retornado ahora incluye `error` | **signature** |
| `src/stores/checkout.ts` | modificado | ninguna (solo JSDoc; `error` ya se exponía) | additive |
| `src/stores/menu.ts` | modificado | ninguna (solo JSDoc) | additive |
| `src/utils/FormatUtil.ts`, `ImageUtil.ts`, `PriceUtil.ts` | modificado | ninguna (solo JSDoc) | additive |
| 13 archivos `*.spec.ts` (nuevos) | agregado | tests, no código de producción | additive |

**Cambio de comportamiento real (el único con blast radius a evaluar):** en `category.ts`, `deal.ts` y `product.ts`, el `error: Ref<string>` interno —declarado desde el commit inicial pero nunca incluido en el `return` del `defineStore(...)`— ahora se expone. Antes, cualquier código que hiciera `store.error` recibía siempre `undefined`, sin importar el resultado real de la última operación fallida.

---

### Blast radius y posibles impactos

**Directos confirmados:** 5 (vistas + `App.vue` que consumen los 3 stores modificados)  |  **Indirectos confirmados:** 1 (`router/index.ts`, solo vía `useCheckoutStore`, no modificado)  |  **Posibles / No verificables:** 0  |  **Profundidad máxima:** 1 nivel

| Módulo / Servicio | Impacto | Evidencia | Criticidad | Justificación |
|---|---|---|---|---|
| `src/views/MenuView.vue` | Directo | Confirmado | Observada | Importa y llama `useCategoryStore()` y `useProductStore()` (líneas 32-33, 53-57) — dos de los tres stores con el cambio de contrato |
| `src/views/HomeView.vue` | Directo | Confirmado | Observada | Importa y llama `useCategoryStore()` y `useDealStore()` (líneas 45-46, 65-69) |
| `src/views/ProductView.vue` | Directo | Confirmado | Observada | Importa y llama `useProductStore()` (líneas 56, 86) |
| `src/views/CheckoutView.vue` | Directo | Confirmado (store no modificado) | Observada | Usa `useCheckoutStore()`, que **no** cambió (ya exponía `error`) — impacto nulo, se incluye solo por trazabilidad |
| `src/App.vue` | Directo | Confirmado (store no modificado) | Observada | Usa `useCheckoutStore()`, sin cambios — impacto nulo |
| `src/router/index.ts` | Indirecto | Confirmado | Observada | El guard `beforeEach` usa `useCheckoutStore()` (store sin cambios); no consume `category`/`deal`/`product` |

**Hallazgo clave para el riesgo:** se buscó `\.error\b` en **todos** los archivos `.vue` del proyecto (`src/views`, `src/components`, `src/App.vue`) y **ninguno** lee hoy `store.error` de `category`, `deal` o `product` — solo `src/components/shared/Input.vue`, `ErrorServer.vue` y `ErrorNotFound.vue` usan `.error`, y ninguno relacionado a estos stores. Por la regla de la skill ("no asumir blast radius para una entidad `additive` sin evidencia de integración"), **no hay ningún consumidor confirmado que dependa de la ausencia previa de `error`** ni que vaya a empezar a leerlo automáticamente: el campo queda disponible, pero nada en el código actual lo consume todavía. Esto reduce el blast radius real efectivo a cero para el comportamiento visible hoy — el riesgo es puramente de superficie de API ampliada, no de regresión.

---

### Cobertura de tests en módulos con impacto directo

| Módulo | Estado | Detalle |
|---|---|---|
| `src/stores/category.ts` | ✅ Cubierto | `src/stores/category.spec.ts` verifica explícitamente `store.error` tras una carga fallida (test "guarda el mensaje de error...") |
| `src/stores/deal.ts` | ✅ Cubierto | `src/stores/deal.spec.ts`, mismo patrón para `loadSpecialProducts` |
| `src/stores/product.ts` | ✅ Cubierto | `src/stores/product.spec.ts`, mismo patrón para `loadProducts` y `loadPreferredProduct` |
| `src/views/MenuView.vue` | ❌ Sin cobertura | No existe ningún test de componente en el proyecto (0% en `src/views`) |
| `src/views/HomeView.vue` | ❌ Sin cobertura | Ídem |
| `src/views/ProductView.vue` | ❌ Sin cobertura | Ídem |
| `src/views/CheckoutView.vue` | ❌ Sin cobertura | Ídem (store no modificado, impacto nulo) |
| `src/App.vue` | ❌ Sin cobertura | Ídem (store no modificado, impacto nulo) |
| `src/router/index.ts` | ❌ Sin cobertura | Ídem (store no modificado, impacto nulo) |

La ausencia de cobertura en `views`/`router` es un riesgo estructural preexistente del proyecto (0% desde antes de este cambio), no algo introducido por este trabajo — se reporta igual porque son los módulos con impacto directo confirmado.

---

### Riesgo global

**Nivel: 🟢 Bajo**

Ningún umbral automático se activó: el único cambio de tipo `signature` (ampliación del `return` de 3 stores) no tiene ningún consumidor `Confirmado` que dependa de él hoy (0 componentes leen `.error` de esos stores), por lo que no aplica el umbral de "API pública afectada con consumidor confirmado → 🔴". Los módulos con impacto directo confirmado son 5, por debajo del umbral de >5 → 🟡. El cambio en sí mismo está cubierto al 100% por los tests nuevos (`category.spec.ts`, `deal.spec.ts`, `product.spec.ts`). El resto del diff es documentación (JSDoc) y configuración de testing, sin riesgo de regresión de producción.

---

### Recomendaciones

1. Ninguna acción de mitigación requerida antes de mergear este trabajo — el cambio de comportamiento real está cubierto por tests y no tiene consumidores actuales.
2. Al construir la próxima vista/componente que necesite mostrar errores de carga de categorías, ofertas o productos, usar `store.error` directamente en `MenuView.vue`/`HomeView.vue`/`ProductView.vue` — ya está disponible y probado; antes de este cambio era imposible.
3. Considerar extender la suite de tests a `src/views` y `src/router` (0% de cobertura) — son los módulos de mayor blast radius potencial del proyecto por ser el punto de entrada de todas las interacciones de usuario, y hoy cualquier regresión ahí es indetectable por tests. Ver también el reporte de deuda técnica (`reports/adeva-tech-debt/local-2026-09-07-2026-09-07.md`) para la priorización formal de este punto.

Puntos a verificar:
- Ninguno — la trazabilidad de consumidores fue directa (grep sobre el 100% de los `.vue` del repositorio), no hubo dependencias `No verificable`.

---

<!-- adeva-impact: risk=low confidence=high modules_direct=5 modules_indirect=1 coverage_gaps=6 -->
